{% extends 'base.html.twig' %}

{% block title %}Smartphones
{% endblock %}

{% block content %}
	<section class="section gallery">
		<div class="heading">
			<h2 class="font-weight-bold">Smartphones</h2>
		</div>

		<div class="container">
			<div class="row">
				<div class="col-md-2">
					<h3>Filtrer</h3>
					{{form(form)}}
					</div>
					<div class="col-md-10 text-center mb-5">
						<div class="row product-container">
							{% for smartphone in smartphones %}
								<div class="col-lg-3 col-md-4 col-sm-6 mb-4">
									<div class="block">
										<div class="gallery-overlay">
											<a href="{{path('app_smartphone', {'slug' : smartphone.slug})}}" class="gallery-popup">
												<i class="fa-solid fa-plus"></i>
											</a>
										</div>
										<img class="img-fluid w-100" src="/uploads/{{smartphone.illustration}}" alt="{{smartphone.name}}" class="img-fluid">
									</div>
									<div class="product-info">
										<a class="link-title" href="{{path('app_smartphone', {'slug' : smartphone.slug})}}">
											<h4>{{smartphone.name}}</h4>
										</a>
										<p>{{smartphone.subtitle}}</p>
										<p class="price">{{(smartphone.price/100) |number_format(2, ',', '.')}}
											€</p>
									</div>
								</div>
							{% endfor %}
						</div>
					</div>
				</div>

			</div>
		</section>
	{% endblock %}
