{% extends 'base.html.twig' %}

{% block title %}Mon Panier
{% endblock %}

{% block content %}
	<section class="section">
		<div class="container">
			<h1>Rectouvez ici l'ensemble des produits que vous avez ajouté à votre panier.</h1>
			<hr>

			<table class="table">
				<thead>
					<tr>
						<th scope="col">Produit</th>
						<th scope="col"></th>
						<th scope="col">Quantité</th>
						<th scope="col">Prox</th>
						<th scope="col">Total</th>
					</tr>
				</thead>
				<tbody>
					{% for product in cart %}
						<tr>
							<th>
								<img src="/uploads/{{product.product.illustration}}" height="50px" alt="{{product.product.name}}">
								<br>
							</th>
							<td>{{product.product.name}}
								<br>
								{{product.product.subtitle}}

							</td>
							<td>{{product.quantity}}</td>
							<td>{{(product.product.price/100) |number_format(2, ',', '.')}}
								€</td>
							<td>{{(product.product.price * product.quantity/100) |number_format(2, ',', '.')}}
								€</td>
						</tr>

					{% endfor %}
				</tbody>
			</table>
			<div class="text-right mb-5">
				<a href="#" class="btn btn-main">Valider mon panier</a>
			</div>
		</div>
	</section>


{% endblock %}
